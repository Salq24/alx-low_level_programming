#include "main.h"

/**
 * _isdigit - Checks if the number is a digit
 * @x: number to be checked
 * Return: 1 if true, 0 if false
 */

int _isdigit(int x)

{
        if (x >= 48 && x <= 57)
        {
        return (1);
        }
        return (0);
 }
